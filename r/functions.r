year <- function(dates) {
	return(as.numeric(format(dates, "%Y")));
}
month <- function(dates) {
	return(as.numeric(format(dates, "%m")));
}
day <- function(dates) {
	return(as.numeric(format(dates, "%d")));
}
